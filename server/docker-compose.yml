version: "3.8"

services:
  database:
    image: postgres
    container_name: dev_up
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=dev_up
